---
"upwork-cli": minor
---

Make multi-query discovery resilient and compact with per-query status, nullable client fields, matched query metadata, CLI version metadata, and a configurable result cap.
